import { StyleSheet, PlatformColor, Platform } from "react-native";

const Colors = StyleSheet.create({
  black: {
    backgroundColor: "#202124",
  },
  ligth: { backgroundColor: "#fff" },
});

export default Colors;
